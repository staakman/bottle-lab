apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hedgedoc
  
resources:
  - database.yaml
  - pooler.yaml
  - deployment.yaml
  - http-route.yaml
  - namespace.yaml
  - service.yaml

commonAnnotations:
  argocd.argoproj.io/sync-options: ServerSideApply=true
